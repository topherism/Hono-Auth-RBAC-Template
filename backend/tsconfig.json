{
  "compilerOptions": {
    /* Language & Module */
    "target": "ESNext",                   // Modern JS features
    "module": "ESNext",                   // Use ES modules
    "moduleResolution": "Bundler",        // Works well with Bun
    "esModuleInterop": true,              // Allows default imports from CommonJS modules (e.g., Pino)
    "resolveJsonModule": true,            // Import JSON files
    "verbatimModuleSyntax": true,         // Preserve import syntax

    /* Strictness & Safety */
    "strict": true,                       // Enable strict type checking
    "forceConsistentCasingInFileNames": true,
    "noUncheckedIndexedAccess": true,     // Catch undefined accesses
    "skipLibCheck": true,                 // Skip type checking for node_modules

    /* Project Structure */
    "baseUrl": ".",                        // Root for path aliases
    "paths": { "@/*": ["./src/*"] },      // Use @/ imports
    "outDir": "./dist"                     // Compiled output
  },
  "include": ["src/**/*.ts"],              // Only compile source files
  "exclude": ["node_modules", "dist"]      // Exclude compiled files and dependencies
}
